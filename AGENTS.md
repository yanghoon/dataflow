- 주변 코드와 유사한 코드를 작성해줘.
- 구현은 최소한으로 하고 설정과 기본 구성값을 사용해서 최소한의 코드로 구현.
- 모든 구현 전에 설계를 먼저, 구현은 설계 확정 및 승인 이후.
- 모든 설계는 공식문서와 현재 사용중인 버전을 기준으로 인터넷과 자료 조사하여 수행.
- Git 커밋 메시지는 Conventional Commits 규약(type: subject)을 따르며, 제목은 50자 이내의 명령형(Imperative) 영문으로 작성해줘.
- 커밋 메시지 본문에는 코드가 '어떻게' 동작하는지보다 '왜' 이 변경을 했고 '무엇'을 바꾸었는지(Why & What)를 중점적으로 최대 200자 이내로 작성해줘. 연관된 이슈가 있다면 이슈 번호를 반드시 포함해줘.
- Java 코드 작성 및 수정 시 FQDN(패키지 경로가 포함된 클래스명)을 인라인으로 직접 사용하지 말고, 반드시 파일 최상단에 `import` 구문을 명시하여 짧은 클래스명을 사용해줘. 기존 코드에 FQDN이 있다면 함께 리팩토링해줘.
- 신규 파일 생성 전, 반드시 주변 폴더를 탐색하여 기존 파일들의 위치와 네이밍 컨벤션(예: 날짜 패턴)을 파악하고 따를 것.
- 임의의 경로에 문서를 만들지 말고, 기존 프로젝트 디렉토리 구조(docs, design 등)와의 정합성을 최우선으로 고려할 것.
