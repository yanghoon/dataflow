workflow:
  jobs:
    subscription-dormant-policy-job:
      type: subscription-dormant-policy
      cron: "0 0 3 * * *"
      props:
        thresholdDays: "20"
        sqlPath: "classpath:sql/subscription_dormant_policy.sql"
        eventSource: "urn:dataflow:policy-service-test"
        eventType: "com.dataflow.subscription.dormant.test"
