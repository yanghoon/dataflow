apiVersion: v1
kind: Service
metadata:
  name: dataflow-ingestion
  labels:
    app: dataflow-ingestion
spec:
  type: ClusterIP
  selector:
    app: dataflow-ingestion
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
