apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # 컨테이너 내부에 마운트될 대시보드 JSON 파일 경로
      path: /etc/grafana/provisioning/dashboards/json
